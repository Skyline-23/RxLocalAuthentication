import XCTest
@testable import RxLocalAuthentication

final class RxLocalAuthenticationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RxLocalAuthentication().text, "Hello, World!")
    }
}
